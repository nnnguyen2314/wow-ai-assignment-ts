import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
    token: {
        fontSize: 12,
        colorPrimary: '#357f9e',
        colorBgContainer: '#ffffff'
    }
};

export default theme;